let express,signUp,login;_b1a‍.w("express",[["default",["express"],function(v){express=v}]]);_b1a‍.w("../controllers/auth",[["signUp",["signUp"],function(v){signUp=v}],["login",["login"],function(v){login=v}]]);
const router = express.Router();

// controllers


// sign up route
router.post("/signup", signUp);

// login route
router.post("/login", login);

module.exports = router;
